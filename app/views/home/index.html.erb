<% content_for :headers do %>
  <style type="text/css" media="screen">
    .btn span{
      font-size:12px;
    }
  </style>
<% end %>
<div class='intro span-24 last'>
  <div class='guide span-8'>
    <div class='words span-7 prepend-3'>
      <p>Welcome! Let's make surveys!</p>
      
      <script type="text/javascript" charset="utf-8">
      function get_cookies_array() {

          var cookies = { };

          if (document.cookie && document.cookie != '') {
              var split = document.cookie.split(';');
              for (var i = 0; i < split.length; i++) {
                  var name_value = split[i].split("=");
                  name_value[0] = name_value[0].replace(/^ /, '');
                  cookies[decodeURIComponent(name_value[0])] = decodeURIComponent(name_value[1]);
              }
          }

          return cookies;

      }

       var cookies = get_cookies_array();

       for(var name in cookies) {

         document.write( name + " : " + cookies[name] + "<br />" );

       }

      </script>
      <!-- <p><a href='/demo' class='btn'><%= t(:try_with_demo) %></a></p> -->
      <p><%= t(:have_an_account) %> <%= link_to t(:login), login_path ,:class => 'ag' %></p>
    </div>    
  </div>


</div>    
